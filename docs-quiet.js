/* Step 3 (stabilize-load-board): neutralized.
   Upload Doc / Manage Docs are rendered natively by app.js renderLoads()
   (Storage upload + signed-URL Manage Docs modal, added in Step 1).
   This file no longer injects any buttons into #loadsList. */
