(()=>{
const by=id=>document.getElementById(id);
const loads=()=>{try{return JSON.parse(localStorage.getItem('loads')||'[]')}catch{return[]}};
const esc=s=>String(s??'').replace(/[<>&"']/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;','"':'&quot;',"'":'&#39;'}[c]));
async function upload(i){const l=loads()[i];if(!l||!l.id)return alert('Sync first.');const inp=document.createElement('input');inp.type='file';inp.accept='image/*,.pdf';inp.onchange=async()=>{const file=inp.files&&inp.files[0];if(!file)return;if(file.size>4.5*1024*1024)return alert('File is too large. Keep it under 4.5 MB for now.');const kind=prompt('Document type: Rate Confirmation, BOL, POD, Lumper Receipt, Invoice Copy, Other','Rate Confirmation')||'Document';const user=(await sb.auth.getSession()).data.session?.user;if(!user)return alert('Login again first.');const reader=new FileReader();reader.onload=async()=>{const r=await sb.from('load_documents').insert({user_id:user.id,load_id:l.id,file_name:'['+kind+'] '+file.name,file_type:file.type||'application/octet-stream',file_data:reader.result});if(r.error)return alert('Upload error: '+r.error.message);alert('Document uploaded. Click Manage Docs to view or delete it.')};reader.readAsDataURL(file)};inp.click()}
async function manage(i){const l=loads()[i];if(!l||!l.id)return alert('Sync first.');const r=await sb.from('load_documents').select('id,file_name,file_data,created_at').eq('load_id',l.id).order('created_at',{ascending:false});if(r.error)return alert(r.error.message);let m=by('zapDocsModal');if(!m){m=document.createElement('div');m.id='zapDocsModal';m.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,.72);z-index:9999;display:flex;align-items:center;justify-content:center;padding:18px';document.body.appendChild(m)}const docs=r.data||[];m.innerHTML='<div class="card" style="width:min(760px,96vw);max-height:88vh;overflow:auto"><div class="section-title"><h2>Load Documents</h2><button class="small-btn" id="zapCloseDocs">Close</button></div><p class="muted">Open/download or delete documents for this load.</p>'+(docs.length?docs.map(d=>'<div class="card" style="margin:10px 0;padding:12px"><b>'+esc(d.file_name||'Document')+'</b><p class="muted">'+esc(new Date(d.created_at).toLocaleString())+'</p><div style="display:flex;gap:8px;flex-wrap:wrap"><a class="small-btn" href="'+d.file_data+'" target="_blank" download="'+esc(d.file_name||'document')+'">Open / Download</a><button class="small-btn zap-delete-doc" data-id="'+esc(d.id)+'">Delete</button></div></div>').join(''):'<p class="muted">No documents saved for this load yet.</p>')+'</div>';by('zapCloseDocs').onclick=()=>m.remove();m.querySelectorAll('.zap-delete-doc').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this document? This cannot be undone.'))return;const del=await sb.from('load_documents').delete().eq('id',b.dataset.id);if(del.error)return alert(del.error.message);alert('Document deleted.');manage(i)})}
function add(){document.querySelectorAll('#loadsList .list-card').forEach((c,i)=>{let a=c.querySelector('.card-actions');if(!a){a=document.createElement('div');a.className='card-actions';c.appendChild(a)}if(!c.querySelector('.zap-upload-doc-btn')){const u=document.createElement('button');u.className='small-btn zap-upload-doc-btn';u.textContent='Upload Doc';u.onclick=()=>upload(i);a.appendChild(u)}if(!c.querySelector('.zap-manage-docs-btn')){const m=document.createElement('button');m.className='small-btn zap-manage-docs-btn';m.textContent='Manage Docs';m.onclick=()=>manage(i);a.appendChild(m)}})}
setInterval(add,1800);
})();