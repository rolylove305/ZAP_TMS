name = "zapdispatch-landing"
compatibility_date = "2025-07-18"

# Cloudflare Pages reads this path relative to the Pages project root.
# In Cloudflare Pages set Root directory to `marketing-site` and Build command
# to `npm run build`; this is the output generated by vinext's static export.
pages_build_output_dir = "dist/client"
