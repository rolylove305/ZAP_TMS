(()=>{
const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
const loads=()=>{try{return JSON.parse(localStorage.getItem('loads')||'[]')}catch{return[]}};
const ok=s=>['delivered','invoiced','paid'].includes(String(s||'').toLowerCase());
const esc=s=>String(s??'').replace(/[<>&]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;'}[c]));
const money=n=>'$'+Number(n||0).toFixed(2);
async function currentUser(){return (await sb.auth.getSession()).data.session?.user}
async function settings(){const u=await currentUser();if(!u)return {};let r=await sb.from('company_settings').select('*').eq('user_id',u.id).maybeSingle();if(r.error)return {};return r.data||{company_name:'Zap Dispatch',invoice_footer:'Thank you for your business.'}}
function loadCompanySettings(){if(q('#companySettingsHelper'))return;const c=document.createElement('script');c.id='companySettingsHelper';c.src='company-settings.js?v=1';document.body.appendChild(c)}
function addTop(){if(q('#invoiceSelectedBtn'))return;const bar=q('#folderBar')||q('#loads .section-title');if(!bar)return;const b=document.createElement('button');b.id='invoiceSelectedBtn';b.className='primary-btn';b.textContent='Invoice selected';b.style.marginTop='10px';b.onclick=invoiceSelected;bar.appendChild(b)}
async function revokeLink(id){if(!confirm('Revoke driver link for this load? The driver portal link will stop working.'))return;const r=await sb.rpc('revoke_driver_link',{p_load_id:id});if(r.error)return alert(r.error.message);alert('Driver link revoked. Generate a new Driver Link if needed.')}
function markCards(){const arr=loads();qa('#loadsList .list-card').forEach((card,i)=>{const l=arr[i];if(!l)return;if(ok(l.status)){let box=card.querySelector('.invoice-select-box');if(!box){box=document.createElement('label');box.className='invoice-select-box';box.style.cssText='display:flex;gap:8px;align-items:center;margin-top:8px;font-weight:800;color:#86efac';box.innerHTML='<input type="checkbox" class="invoice-select"> Select for invoice';card.prepend(box)}box.querySelector('input').dataset.id=l.id}let acts=card.querySelector('.card-actions');if(!acts){acts=document.createElement('div');acts.className='card-actions';card.appendChild(acts)}if(!card.querySelector('.revoke-link-btn')){const b=document.createElement('button');b.className='small-btn revoke-link-btn';b.textContent='Revoke Link';b.onclick=()=>revokeLink(l.id);acts.appendChild(b)}})}
async function invoiceSelected(){const ids=qa('.invoice-select:checked').map(x=>x.dataset.id).filter(Boolean);if(!ids.length)return alert('Select at least one load.');const u=await currentUser();if(!u)return alert('Login again first.');const r=await sb.from('loads').select('*').in('id',ids).order('delivery_date',{ascending:true});if(r.error)return alert(r.error.message);const items=r.data||[];if(!items.length)return alert('No loads found.');const carriers=[...new Set(items.map(x=>x.carrier||''))];if(carriers.length>1)return alert('Select loads from only one carrier.');const st=await settings();const num=await sb.rpc('get_next_invoice_number');if(num.error)return alert(num.error.message);const invoiceNumber=num.data;let total=0;const rows=items.map(x=>{const rate=Number(x.rate||0),pct=Number(x.commission_pct||st.default_commission_pct||0),due=rate*pct/100;total+=due;return '<tr><td>'+esc(x.load_number||'-')+'</td><td>'+esc((x.pickup||'')+' → '+(x.delivery||''))+'</td><td>'+esc(x.delivery_date||x.pickup_date||'')+'</td><td>'+money(rate)+'</td><td>'+pct+'%</td><td>'+money(due)+'</td></tr>'}).join('');const carrier=carriers[0]||'Carrier';const inv=await sb.from('invoices').insert({user_id:u.id,invoice_number:invoiceNumber,carrier,total}).select('id').single();if(inv.error)return alert(inv.error.message);await sb.from('invoice_loads').insert(items.map(x=>{const rate=Number(x.rate||0),pct=Number(x.commission_pct||st.default_commission_pct||0);return {invoice_id:inv.data.id,load_id:x.id,amount_due:rate*pct/100}}));const logo=st.logo_url?'<img src="'+esc(st.logo_url)+'" style="max-height:65px;max-width:180px;margin-bottom:10px">':'';const contact=[st.email,st.phone].filter(Boolean).map(esc).join('<br>');const pay=st.zelle_info?esc(st.zelle_info):'Zelle payment details not set.';const html='<!doctype html><html><head><title>'+esc(invoiceNumber)+' - '+esc(carrier)+'</title><style>body{font-family:Arial,sans-serif;color:#111;padding:30px}h1{margin:0}table{width:100%;border-collapse:collapse;margin-top:22px}td,th{border:1px solid #ccc;padding:9px;font-size:13px;text-align:left}th{background:#f2f2f2}.top{display:flex;justify-content:space-between;gap:20px}.total{text-align:right;font-size:22px;font-weight:800;margin-top:18px}.pay{margin-top:28px;padding:14px;border:1px solid #ddd;background:#fafafa}.btn{margin:18px 0;padding:10px 14px;border:0;background:#0f766e;color:white;border-radius:8px}.muted{color:#555}@media print{.btn{display:none}}</style></head><body><button class="btn" onclick="window.print()">Print / Save as PDF</button><div class="top"><div>'+logo+'<h1>'+esc(st.company_name||'Zap Dispatch')+'</h1><p class="muted">'+contact+'</p></div><div><h2>INVOICE</h2><b>Invoice #:</b> '+esc(invoiceNumber)+'<br><b>Date:</b> '+new Date().toLocaleDateString()+'<br><b>Carrier:</b> '+esc(carrier)+'</div></div><table><thead><tr><th>Load #</th><th>Lane</th><th>Date</th><th>Rate</th><th>Dispatch %</th><th>Amount Due</th></tr></thead><tbody>'+rows+'</tbody></table><div class="total">Total Due: '+money(total)+'</div><div class="pay"><b>Payment Info:</b><br>'+pay+'</div><p class="muted">'+esc(st.invoice_footer||'Thank you for your business.')+'</p></body></html>';const w=window.open('','_blank');if(!w)return alert('Popup blocked. Allow popups for this site.');w.document.write(html);w.document.close();setTimeout(()=>w.print(),700);if(confirm('Invoice '+invoiceNumber+' created. Mark selected loads as Invoiced?')){const up=await sb.from('loads').update({status:'Invoiced'}).in('id',ids);if(up.error)alert(up.error.message);if(typeof loadCloud==='function')await loadCloud()}qa('.invoice-select:checked').forEach(x=>x.checked=false)}
setInterval(()=>{loadCompanySettings();addTop();markCards()},1500);
})();