# Local tooling / scratch — never commit
.claude/
node_modules/
supabase/.temp/
.wrangler/

# Admin-only local tools — not part of the deployed app
admin-eld-requests.html

# Secrets — must never be committed
*.pem
*.key
.env
.env.*
supabase-token*
service-role*
