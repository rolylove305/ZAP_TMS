# Local tooling / scratch — never commit
.claude/
node_modules/
supabase/.temp/
.wrangler/

# Secrets — must never be committed
*.pem
*.key
.env
.env.*
supabase-token*
service-role*

.dev.vars*
!.dev.vars.example
!.env.example
