/* DEPRECATED — the old invite-only "Invite Users" panel (app_invites) was
   removed when signup became open (self-registration + 30-day trial). User
   administration now lives in the "Admin" button / admin-users.js (view all
   profiles, deactivate/reactivate). This file is intentionally a no-op so the
   obsolete panel is never injected; kept as a stub so cached loaders that still
   request it do not 404. */
(()=>{})();
